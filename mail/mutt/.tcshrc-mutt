# create mutt commands for every account in ~/.mutt
# copy it to ${home}
if ( -d ~/.mutt ) then
	if ( `ls ~/.mutt/*` != "" ) then
		foreach f ( ~/.mutt/* )
			set name = $f:t
			alias mutt-$name "mutt -F ~/.mutt/$name"
		end
	endif
endif
